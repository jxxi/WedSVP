import React from 'react';

var UserProfile = React.createClass({
    render: function() {
      return (
        <div>
          user profile page
        </div>
      );
    }
});

export default UserProfile;